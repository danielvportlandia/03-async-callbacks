'use strict';

const what = 'what';
console.log(what);

describe('initial test to make sure Travis works', () => {
  it('should show that Travis is operating properly', () => {
    expect(true).toEqual(true);
  });
});


